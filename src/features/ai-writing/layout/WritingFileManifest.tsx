'use client';
import { Check, FileText } from 'lucide-react';
import type { WritingDocumentState } from '../writingDocumentTypes';
export default function WritingFileManifest({ document, open }: { document: WritingDocumentState; open: boolean }) { if (!open) return null; const files=[{name:`大纲｜${document.title}`,detail:`${document.outline.length} 个结构节点`},{name:`正文｜${document.title}`,detail:`${document.generatedLength} 字`},...(document.layoutStatus==='formatted'?[{name:`排版｜${document.title}`,detail:'智能排版文档'}]:[])]; return <div className="absolute right-0 top-11 z-50 max-h-[70vh] w-80 overflow-y-auto rounded-2xl border border-slate-200 bg-white p-3 shadow-xl">{files.map((file,index)=><div key={file.name} className={`flex gap-3 rounded-xl p-3 ${index===files.length-1?'bg-slate-50':''}`}><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-blue-50 text-blue-600">{index===files.length-1?<Check size={15}/>:<FileText size={15}/>}</span><span className="min-w-0"><strong className="block truncate text-sm">{file.name}</strong><span className="text-xs text-slate-400">{file.detail}</span></span></div>)}</div>; }
