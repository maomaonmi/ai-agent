# 千问（Qwen）接入计划

> 版本：V1.0 · 2026-08-06
> 决策基线：① 输入框下拉 = 全局 active profile 切换（沿用现有 ModelQuickSwitcher 语义）；② 专项生成模型（文生图/文生视频）后置，本轮仅落地聊天 + Code 沙箱主链路。

---

## 1. 目标范围

| 纳入本轮 | 后置（独立迭代） |
|---|---|
| 设置界面千问预设 + 完整官方模型目录 | 文生图 / 文生视频（DashScope 原生异步任务 API） |
| 千问文本 / 视觉模型接入 `/chat` 与 Code 沙箱全链路 | 生成面板、任务进度 SSE、session 配额计数 |
| 输入框下拉选模型 + 思考强度（正式聊天 + Code 沙箱） | |
| 千问专属参数调节（enable_thinking / thinking_budget） | |
| GLM 字符串嗅探 → 供应商能力矩阵收口 | |

**协议事实**：千问 OpenAI 兼容模式 `base_url=https://dashscope.aliyuncs.com/compatible-mode/v1`，环境变量 `DASHSCOPE_API_KEY`；文本/视觉走 chat completions，专项生成不在兼容协议内（本轮不碰）。

---

## 2. 千问模型目录（预置）

| 分组 | model_id | 能力标记 | 适用场景 |
|---|---|---|---|
| 旗舰 | `qwen3.8-max` | thinking + budget | 全栈生成、疑难修复、任务规划 |
| 均衡 | `qwen3.7-plus` | thinking + budget | 复杂补丁、多智能体讨论（默认） |
| 性价比 | `qwen3.7-flash` | thinking（轻量轮次用） | 子任务补丁、高频调用 |
| 视觉 | `qwen-vl-max` 等 VL 系列 | vision | 截图分析、附件理解 |

变体元数据结构（前后端共用契约）：

```typescript
interface ModelVariant {
  value: string;            // 'qwen:qwen3.8-max' 复合值编码
  label: string;
  modelId: string;
  provider: 'qwen';
  supportsVision: boolean;
  thinkingControl: 'budget' | 'toggle' | 'none'; // 千问=budget；GLM=toggle+effort
  inputContext: number;
  outputContext: number;
}
```

**目录收口决策**：模型目录目前在前端硬编码两份（SettingsDialog 与 ModelQuickSwitcher，代码注释已承认需手动同步）。本轮新增后端目录接口，前端两处统一从接口取，杜绝三份硬编码腐化。

---

## 3. 改造点清单

### 3.1 配置层 — `model_settings.py`

- [ ] `provider` 枚举增加 `"qwen"`（同步改 `frontend/ai-agent/src/lib/api.ts` 的 `ModelSettings['provider']` 联合类型）
- [ ] 新增字段：`enable_thinking: bool = True`、`thinking_budget: int | None = None`（`reasoning_effort` 保留给 GLM，互不复用）
- [ ] `ModelSettingsStore.load` 补 qwen 默认 profile：`base_url=https://dashscope.aliyuncs.com/compatible-mode/v1`、`model_id=qwen3.7-plus`、`display_name=千问 Qwen3.7 Plus`
- [ ] 新增 `PROVIDER_CAPABILITIES` 映射：model_id 前缀 → `(supports_json_format, thinking_control, supports_vision)`，**收口全部 `"glm" in model.lower()` 嗅探**
- [ ] 新增 `GET /api/settings/model-catalog` 接口，返回分供应商的变体目录（含能力元数据）

### 3.2 后端聊天链 — `main.py`

- [ ] 附件门禁（现 L1984 `provider != "glm"` → 422）：改为按能力 `supports_vision` 判断，放行 qwen-vl 系列
- [ ] GLM 专属分支（现 L1992）：`generate_glm_chat_events` 泛化为 `generate_direct_chat_events`，按 provider 分发 `extra_body`：
  - GLM：`{"thinking": {"type": "enabled"|"disabled", "reasoning_effort": ...}}`
  - 千问：`{"enable_thinking": bool, "thinking_budget": int}`
- [ ] `settings_provider`（现 L1759 四元组）：改为返回完整 `ModelSettings` 对象（内部接口，可控 breaking change），否则 thinking_budget 无法传入 Code 链路
- [ ] 错误映射：401/403 → 密钥无效；429 → 限流/额度不足；400 + thinking 参数错误 → 明确提示

### 3.3 后端 Code 链 — `App.py`

- [ ] L392 `uses_json_format = "glm" not in model.lower()` → 能力查询（千问保留 `json_object`）
- [ ] L407-L412 thinking 注入 → 按能力分发千问 `enable_thinking` + `thinking_budget`
- [ ] L5345 Code 附件门禁 `"glm" not in model_name.lower()` → 能力判断
- [ ] 保留现有三级兜底：流式 content 为空 → 非流式重试 → reasoning 兜底（对千问同样有效）
- [ ] `thinking_budget` 校验：必须 < `max_tokens`，防止 budget 挤占导致 content 为空（GLM 已有同类前科）

### 3.4 前端

- [ ] `ModelQuickSwitcher.tsx`：
  - GLM_VARIANTS 旁接入千问变体（数据来自 catalog 接口）
  - 思考控件由**当前变体能力**驱动显隐与语义：千问显示 budget 档位（轻 2k / 标准 8k / 深 16k token），GLM 保持 effort 七档
  - 切换写入对应 provider profile（保持全局切换语义）
- [ ] `SettingsDialog.tsx`：
  - PRESETS 增加 qwen
  - 模型 ID 下拉接入千问官方目录（catalog 接口）
  - 参数区按 provider 条件渲染：千问显示 `enable_thinking` / `thinking_budget`，隐藏 `reasoning_effort`
- [ ] `ChatInterface.tsx` / `CodeWorkspace.tsx`：**零改动**（modelControl 已注入，直接受益）

### 3.5 架构红线

- 新增供应商能力判断一律走 `PROVIDER_CAPABILITIES`，**禁止再出现 `"xxx" in model.lower()` 字符串嗅探**

---

## 4. 单测计划

| 用例 | 断言 |
|---|---|
| 千问 profile 下发起 chat completions | `extra_body` 含 `enable_thinking`/`thinking_budget`，**不含** `thinking`/`reasoning_effort` |
| GLM profile 回归 | `extra_body` 格式与现状完全一致 |
| 千问 + `json_object` + stream | 保留 response_format；mock content 为空时走非流式重试 |
| 附件 + qwen 视觉模型 | 放行；附件 + qwen 纯文本模型 → 422 |
| 附件 + GLM（回归） | 行为不变 |
| `thinking_budget >= max_tokens` | 校验拒绝并提示 |
| catalog 接口 | 返回结构含全部分组与能力标记 |
| 前端思考控件 | 千问变体渲染 budget 档、GLM 变体渲染 effort 档（组件测试） |

测试文件：`tests/test_model_settings.py` 扩展 + 新增 `tests/test_qwen_provider.py`。

---

## 5. 风险与规避

| 风险 | 等级 | 规避 |
|---|---|---|
| GLM 格式 thinking 参数误传千问 → 400 | 高 | 能力矩阵强制分发 + 单测断言 create_kwargs |
| thinking_budget 挤占 max_tokens 致 content 为空 | 高 | budget < max_tokens 校验 + 三级兜底 |
| 千问 `json_object + stream` 行为未实测 | 中 | 接入后先跑一次全栈生成实测；兜底链已具备 |
| 千问进 LangGraph 多智能体讨论链 tool_call 表现未知 | 中 | 本轮千问仅进 direct chat 分支；讨论链实测后另行放开 |
| 模型目录多份硬编码腐化 | 中 | catalog 接口收口为单一数据源 |
| Key 泄露 | 高 | 环境变量注入；`public()` 排除 api_key 回传前端（现状保持） |
| GLM 行为回归 | 中 | 能力矩阵 1:1 保留 GLM 现有行为 + 回归单测 |

---

## 6. 实施顺序

1. 配置层：枚举 + qwen profile + 能力矩阵 + catalog 接口
2. 后端链路：`/chat` 分支泛化 → `App.py` 三处嗅探收口 → settings_provider 改传完整对象
3. 前端：QuickSwitcher 千问目录 + 能力驱动思考控件 → SettingsDialog 预设 + 条件参数区
4. 单测 + GLM 回归验证
5. 实测验收：千问跑一次全栈生成 + 带图对话 + budget 三档各验证一次

## 7. 验收标准

- 设置界面可选千问预设，保存后两个输入框下拉出现千问模型分组
- 千问视觉模型带图对话成功；纯文本模型带图返回 422 且提示明确
- 思考 budget 三档生效，GLM effort 七档回归无变化
- Code 沙箱千问全栈生成跑通，SSE 流式正常
- 全部新单测通过，GLM 现有测试无回归
