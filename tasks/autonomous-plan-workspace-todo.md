# Autonomous Plan Workspace TODO

- [x] Add malformed-GLM plan fixtures and parser tests.
- [x] Add `PlanWorkspace` and report document components.
- [x] Add plan message persistence during `plan_update` and `done`.
- [x] Add right-pane mount for both plan modes without changing `sendChatMessage`.
- [x] Add report table/chart/image rendering and action toolbar.
- [x] Run focused Python tests and TypeScript check; record pre-existing lint/build blockers.
