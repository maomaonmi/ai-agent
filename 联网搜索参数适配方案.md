# 联网搜索参数适配方案

> 状态：待用户确认 | 范围：千问原生搜索参数全量接入 + GLM count 默认值修正 + 前端转圈问题修复

## 一、背景与目标

### 1.1 问题
- 千问联网搜索参数硬编码（仅 `search_strategy=turbo`），未接入官方支持的全部能力
- GLM `count=5` 硬编码，未达"至少 10 条相关结果"目标
- 前端无千问专属参数调节面板，DeepSeek 的 Firecrawl 面板对 GLM/Qwen 不生效（已标"仅 DeepSeek 生效"）
- 直连路径（GLM/Qwen）可能存在前端转圈不停止问题

### 1.2 目标
- 千问：接入 OpenAI 兼容 Chat Completions 协议支持的全部原生搜索参数
- 前端：在输入框旁加 Popover 按钮（仅千问模型可见），可调节所有原生搜索参数
- GLM：仅将 `count` 默认值从 5 改为 10（可配置），其他参数保持现状
- 修复：直连路径前端转圈问题

### 1.3 约束
- 不动 DeepSeek 链路（Firecrawl/Tavily/Rerank 保持原样）
- GLM 不做独立 UI 面板
- 参数会话级持久化（随 SessionSnapshot 保存/恢复）
- 遵循现有三态 merge、GET 脱敏等既有约定

---

## 二、千问官方文档差距分析

### 2.1 协议能力矩阵（OpenAI 兼容 Chat Completions）

| 功能 | 当前是否接入 | 参数 | 说明 |
|---|---|---|---|
| 基础联网搜索 | ✅ | `enable_search: true` | 已接入 |
| 强制联网搜索 | ❌ | `forced_search: true` | 强制执行搜索，不依赖模型判断 |
| 搜索量级策略 | ⚠️ 写死 turbo | `search_strategy`: turbo/max/agent/agent_max | agent 系列额外收费 |
| 垂域搜索 | ❌ | `enable_search_extension: true` | 天气/股票/汇率等结构化数据 |
| 搜索时效性 | ❌ | `freshness`: 7/30/180/365 | 仅 turbo 生效，单位天 |
| 限定来源站点 | ❌ | `assigned_site_list`: [array] | 仅 turbo 生效，最多 25 个站点 |
| 自然语言控制检索范围 | ❌ | `prompt_intervene`: string | 仅 turbo 生效 |
| 返回搜索来源 | ⚠️ 已设但无效 | `enable_source: true` | **OpenAI 兼容协议不支持**，仅 DashScope 生效 |
| 角标引用标注 | ⚠️ 已设但无效 | `enable_citation` / `citation_format` | **OpenAI 兼容协议不支持** |

### 2.2 关键发现
当前代码在 `main.py:1524-1541` 和 `main.py:3907-3918` 两处注入了 `enable_source` / `enable_citation` / `citation_format`，但根据官方文档明确声明：**OpenAI 兼容协议（Chat Completions）不支持这些参数**。这些参数在当前 OpenAI 兼容协议下是无效的，应清理。

### 2.3 search_strategy 策略说明
- `turbo`（默认）：兼顾速度与效果，支持全部子参数
- `max`：多源搜索引擎，结果更详尽，响应更长
- `agent`：多轮检索，模型自主决定搜索时机，**仅支持 enable_source**，其他参数失效，额外收费
- `agent_max`：agent + 网页全文阅读，**仅 qwen3-max 思考模式可用**，额外收费

---

## 三、后端改动方案

### 3.1 新增 QwenNativeSearchOptions 模型

**位置**：`main.py`（在 `WebSearchOptions` 附近，约 328 行后）

```python
class QwenNativeSearchOptions(BaseModel):
    """千问原生联网搜索参数（OpenAI 兼容 Chat Completions 协议）。
    
    Why: 千问走 OpenAI 兼容协议，不走 web_search_node。这些参数通过 extra_body
    注入，与 DeepSeek 的 WebSearchOptions 完全独立。
    """
    search_strategy: Literal["turbo", "max", "agent", "agent_max"] = "turbo"
    forced_search: bool = False
    enable_search_extension: bool = False
    freshness: Literal[0, 7, 30, 180, 365] = 0  # 0=不限，仅 turbo 生效
    assigned_site_list: list[str] = Field(default_factory=list, max_length=25)  # 仅 turbo 生效
    prompt_intervene: str = Field(default="", max_length=200)  # 仅 turbo 生效
```

### 3.2 RuntimeSettings 扩展

**位置**：`main.py:363`（`RuntimeSettings` 类）

```python
class RuntimeSettings(BaseModel):
    # ... 既有字段 ...
    web_search_options: WebSearchOptions = Field(default_factory=WebSearchOptions)
    qwen_native_search_options: QwenNativeSearchOptions = Field(default_factory=QwenNativeSearchOptions)  # 新增
```

### 3.3 注入逻辑统一（两处）

**位置 1**：`main.py:1524-1541`（`chat_node` 内）
**位置 2**：`main.py:3907-3918`（`generate_direct_chat_events` 内）

**改动**：从硬编码改为读取 `state["qwen_native_search_options"]` / `settings.qwen_native_search_options`，构造 `search_options` 字典。

```python
def _build_qwen_search_options(opts: QwenNativeSearchOptions) -> dict:
    """构造千问 search_options 字典。
    
    Why: agent/agent_max 策略下仅 enable_source 生效，其他参数会被服务端忽略，
    但本地主动过滤避免无效参数传输，且符合官方文档约定。
    """
    search_options: dict = {"search_strategy": opts.search_strategy}
    
    if opts.search_strategy in ("turbo", "max"):
        if opts.forced_search:
            search_options["forced_search"] = True
        if opts.enable_search_extension:
            search_options["enable_search_extension"] = True
        if opts.search_strategy == "turbo":
            if opts.freshness > 0:
                search_options["freshness"] = opts.freshness
            if opts.assigned_site_list:
                search_options["assigned_site_list"] = opts.assigned_site_list
            if opts.prompt_intervene.strip():
                search_options["intention_options"] = {
                    "prompt_intervene": opts.prompt_intervene.strip()
                }
    return search_options
```

注入点（两处统一）：
```python
elif cur_provider == "qwen":
    if extra_body is None:
        extra_body = {}
    qwen_opts = state.get("qwen_native_search_options")  # 或 settings.qwen_native_search_options
    extra_body.update({
        "enable_search": True,
        "search_options": _build_qwen_search_options(qwen_opts),
    })
    # 清理无效参数：OpenAI 兼容协议不支持 enable_source/enable_citation/citation_format
```

### 3.4 GLM count 默认值修正

**位置**：`main.py:1503-1517` 和 `main.py:3892-3906`

```python
# 改动前
"count": 5,
# 改动后
"count": 10,  # 从 5 提升到 10，符合"至少 10 条相关结果"目标
```

### 3.5 LangGraph state 扩展

**位置**：`main.py:259-263`（`GroundedState` TypedDict）

```python
class GroundedState(TypedDict):
    # ... 既有字段 ...
    qwen_native_search_options: dict  # 新增
```

**位置**：`main.py:3083-3085`（`generate_chat_events` 构造 inputs）

```python
inputs = {
    # ... 既有字段 ...
    "qwen_native_search_options": settings.qwen_native_search_options.model_dump(),
}
```

---

## 四、前端改动方案

### 4.1 类型定义

**位置**：`frontend/ai-agent/src/lib/api.ts`（在 `WebSearchOptions` 附近）

```typescript
export interface QwenNativeSearchOptions {
  searchStrategy: 'turbo' | 'max' | 'agent' | 'agent_max';
  forcedSearch: boolean;
  enableSearchExtension: boolean;
  freshness: 0 | 7 | 30 | 180 | 365;  // 0=不限
  assignedSiteList: string[];
  promptIntervene: string;
}

export const DEFAULT_QWEN_NATIVE_SEARCH_OPTIONS: QwenNativeSearchOptions = {
  searchStrategy: 'turbo',
  forcedSearch: false,
  enableSearchExtension: false,
  freshness: 0,
  assignedSiteList: [],
  promptIntervene: '',
};
```

### 4.2 请求体透传

**位置**：`frontend/ai-agent/src/lib/api.ts:660-680`（`sendChatMessage`）

```typescript
qwen_native_search_options: {
  search_strategy: options.runtimeSettings.qwenNativeSearchOptions?.searchStrategy,
  forced_search: options.runtimeSettings.qwenNativeSearchOptions?.forcedSearch,
  enable_search_extension: options.runtimeSettings.qwenNativeSearchOptions?.enableSearchExtension,
  freshness: options.runtimeSettings.qwenNativeSearchOptions?.freshness,
  assigned_site_list: options.runtimeSettings.qwenNativeSearchOptions?.assignedSiteList,
  prompt_intervene: options.runtimeSettings.qwenNativeSearchOptions?.promptIntervene,
},
```

### 4.3 RuntimeSettings 类型扩展

**位置**：`frontend/ai-agent/src/lib/api.ts`（`RuntimeSettings` 接口）

```typescript
export interface RuntimeSettings {
  // ... 既有字段 ...
  qwenNativeSearchOptions?: QwenNativeSearchOptions;
}
```

### 4.4 新增 QwenSearchOptionsPopover 组件

**位置**：`frontend/ai-agent/src/components/QwenSearchOptionsPopover.tsx`（新建）

**Props**：
```typescript
interface QwenSearchOptionsPopoverProps {
  options: QwenNativeSearchOptions;
  onChange: (options: QwenNativeSearchOptions) => void;
  onReset: () => void;
}
```

**UI 结构**（Popover 弹出层）：
- 触发器：地球+齿轮图标按钮（仅在 provider === 'qwen' 且 webSearch !== 'off' 时显示）
- 弹出内容（宽 320px）：
  1. **搜索策略** SegmentedControl：turbo / max / agent / agent_max
     - agent / agent_max 选项下方显示橙色警告条："⚠️ 此策略每次调用额外收费，且仅 enable_source 生效"
     - agent_max 仅 qwen3-max 思考模式可用，非该模型时置灰
  2. **强制搜索** Switch：开启后模型必定执行搜索
  3. **垂域搜索** Switch：天气/股票/汇率等结构化数据
  4. **时效性** Select：不限/7天/30天/180天/365天（仅 turbo 生效，非 turbo 时置灰）
  5. **限定站点** Tag 输入框：最多 25 个域名（仅 turbo 生效）
  6. **检索范围引导** Textarea：prompt_intervene 自然语言（仅 turbo 生效，最多 200 字）
  7. **恢复默认** 按钮

**置灰规则**：
- 非 turbo 策略下，时效性/限定站点/检索范围引导三项置灰
- agent 策略下，强制搜索/垂域搜索也置灰（仅 enable_source 生效）

### 4.5 ChatInterface 集成

**位置**：`frontend/ai-agent/src/components/ChatInterface.tsx`

```typescript
// 状态管理（与 webSearchOptions 并列）
const [qwenNativeSearchOptions, setQwenNativeSearchOptions] = 
  useState<QwenNativeSearchOptions>(DEFAULT_QWEN_NATIVE_SEARCH_OPTIONS);

// 会话快照保存/恢复（与 webSearchOptions 并列）
// SessionSnapshot 接口新增 qwenNativeSearchOptions?: QwenNativeSearchOptions;
```

**按钮放置位置**：输入框工具栏，在"🌐 联网"开关旁边，仅在 `provider === 'qwen'` 时渲染。

---

## 五、前端转圈问题修复

### 5.1 现状
调研报告已指出 4 个根因，其中 3 个已有修复机制：
1. ✅ 同节点 processing→completed 自动更新（`ChatInterface.tsx:115-140`）
2. ✅ LangGraph 节点未显式 completed 时兜底补发（`main.py:3136-3145`）
3. ✅ start 节点补发 completed（`main.py:3103-3109`）
4. ✅ node 事件之间 50ms 让出（`api.ts:786-789`）

### 5.2 待修复：直连路径（GLM/Qwen）的进度闭环

**位置**：`main.py:3886-4075`（`generate_direct_chat_events`）

**问题**：直连路径推送"原生搜索已启用"的 `node` 事件后，可能在模型输出完成前/后未补发 `completed` 事件，导致前端进度面板对应节点永久 processing。

**修复**：在 `generate_direct_chat_events` 的 done 事件前，对已推送过 processing 但未推送 completed 的节点，统一补发 completed：

```python
# main.py generate_direct_chat_events 末尾，done 事件之前
# 记录已推送 processing 的节点名集合
_pushed_processing_nodes: set[str] = set()
# ... 在推送 processing 事件时同步加入集合 ...
# done 前：
for node_name in _pushed_processing_nodes:
    yield sse_format("node", {
        "node_name": node_name,
        "status": "completed",
        "message": f"{node_name} 执行完成",
    })
    await asyncio.sleep(0)
```

### 5.3 isLoading 状态兜底

**位置**：`frontend/ai-agent/src/components/ChatInterface.tsx`

直连路径的 `finally` 块已覆盖 `setIsLoading(false)`，但需确认所有 chat 路径（standard/deep/web/research/plan/agent/code）的 `finally` 都到位。调研报告显示 `ChatInterface.tsx:919/952/987/1063` 四处 `finally` 已覆盖。

---

## 六、实施步骤

### 阶段 1：后端（千问参数模型 + 注入逻辑）
1. `main.py` 新增 `QwenNativeSearchOptions` Pydantic 模型
2. `RuntimeSettings` 新增 `qwen_native_search_options` 字段
3. `GroundedState` 新增 `qwen_native_search_options` 字段
4. 新增 `_build_qwen_search_options()` 辅助函数
5. 改造 `chat_node` 和 `generate_direct_chat_events` 两处注入逻辑
6. 清理无效的 `enable_source` / `enable_citation` / `citation_format` 参数
7. GLM `count` 默认值 5→10（两处）
8. `generate_chat_events` 构造 inputs 时透传新字段

### 阶段 2：前端（类型 + 请求透传）
1. `api.ts` 新增 `QwenNativeSearchOptions` 接口和默认值
2. `RuntimeSettings` 接口新增字段
3. `sendChatMessage` 请求体透传
4. `SessionSnapshot` 接口新增字段

### 阶段 3：前端（UI 组件）
1. 新建 `QwenSearchOptionsPopover.tsx`
2. `ChatInterface.tsx` 新增 state + 快照保存/恢复
3. 输入框工具栏集成按钮（条件渲染：provider==='qwen'）

### 阶段 4：修复前端转圈
1. `generate_direct_chat_events` 补发 completed 事件
2. 验证直连路径进度面板闭环

---

## 七、验证清单

- [ ] 千问 turbo 策略：默认参数下搜索正常，回答含角标
- [ ] 千问 max 策略：切换后响应变慢但结果更详尽
- [ ] 千问 agent 策略：显示收费警告，其他参数置灰
- [ ] 千问 freshness=7：仅返回 7 天内结果
- [ ] 千问 assigned_site_list：限定指定站点
- [ ] 千问 prompt_intervene：自然语言引导生效
- [ ] 千问 enable_search_extension：垂域数据返回
- [ ] 千问 forced_search：强制执行搜索
- [ ] GLM count=10：返回 10 条结果
- [ ] Popover 仅千问模型可见
- [ ] 参数会话级持久化：刷新/切换会话后恢复
- [ ] 直连路径进度面板闭环：模型输出完后不转圈
- [ ] DeepSeek 链路无回归

---

## 八、文件改动清单

### 后端
- `main.py` — 新增模型 + 注入逻辑 + state 扩展 + GLM count 修正 + 直连路径 completed 补发

### 前端
- `frontend/ai-agent/src/lib/api.ts` — 类型定义 + 请求透传 + 快照接口
- `frontend/ai-agent/src/components/QwenSearchOptionsPopover.tsx` — **新建**
- `frontend/ai-agent/src/components/ChatInterface.tsx` — state + 快照 + 按钮集成

### 不动文件
- `model_settings.py` — ServiceSettings 不变（千问原生搜索参数走会话级，不走全局服务设置）
- `glm_adapter.py` — 与联网搜索无关
- `RuntimeSettingsDrawer.tsx` — DeepSeek 的 Firecrawl 面板保持原样
- `SettingsDialog.tsx` — 全局服务设置不变
