## 根本原因
截图显示：新对话的 index.html 里， 红色删除行是上一个会话的代码 ，绿色新增行是 {} 。

问题链路 ：

1. 用户点"新建对话" → resetConversation() → resetCode() → code = ''
2. fullstack_generate_stream 开始 → 先发 code_update: "{}" 占位（ App.py#L2269 ）
3. 前端 useEffect([code]) 把 {} parse 成空 VFS → setVfs({})
4. 但 previousFileSnapshotsRef 还残留着上一个会话的文件内容 ，用于 diff 计算
5. 所以 diff 显示：旧 VFS 的所有文件被"删除"，新 VFS 是空的 → 满屏红色
核心 Bug ： resetCode() 没有清理 CodeWorkspace 内部的 previousFileSnapshotsRef （diff 历史快照）。

### 1. 核心结论/代码
修复了“流式输出无换行 → 源码单行 → 预览无界面”的连锁问题，共改 2 个文件：

后端 App.py ：

- 新增 _fix_content_newlines() 统一换行修复器 （L236-L255）：启发式识别“字面量 \n 多、真实换行少”的双重转义，自动还原为真实换行。并在 clean_generated_vfs 、 normalize_agent_envelope 的 4 条早返回路径里全量覆盖：
  
  - 单 HTML 重写 ( payload.html )
  - 精确补丁 ( payload.operations[].content )
  - file-replace 批量替换 ( payload.files )
  - fullstack_bootstrap（顶层路径 key = 文件内容）
  - 旧纯前端 _looks_like_full_html 裸字符串路径
- 删除 6 处流式输出重复 yield ：原来 for ev in sse_events: yield ev 之后又 yield format_sse(channel=output, content=accumulated) ，导致完整文本被 再次追加 到流式 chunks 后面 → trace.output 变成 chunk1..chunkN + fullText 的重复串， formatModelOutput 的 JSON.parse 永远失败，永远退化为单行裸字符串。删除后 stream_json_completion 的 fallback 分支（stream=False 时 sse_events 本身已含完整文本）与真·流式分支口径一致。
- 系统提示词新增 NEWLINE RULES ： FULLSTACK_GENERATE_SYSTEM_PROMPT （L496-L504）和 FULLSTACK_PATCH_SYSTEM_PROMPT 第 9 条（L561-L569）明确要求：每条 CSS 规则、每个 JS 语句块、每个块级标签后必须写真实 \n 转义； // 注释必须独占一行（正是导致预览无界面的根因——单行里 // 会吞掉后续所有代码）。
前端 CodeWorkspace.tsx ：

- 升级 formatModelOutput() （L110-L143）： JSON.parse 失败（流式阶段 JSON 尚未闭合）时不再原样返回 compact 单行，而是对 }, ], {" [" ":{ ": [ }} }] 等结构 token 注入换行；同时按 (\w)",\s*" 再拆顶层 key。让“完整模型输出”大黑框在流式途中也能分段显示，不再是一面 2000 字符的墙。

### 核心结论
两处协同根因 ，修复 3 个点：
 1. 后端根因（最直接触发点）
App.py L2422 在 fullstack 生成前先 yield 占位 code_update: "{}" 。如果后续模型返回空 JSON {} 或异常，except 分支没有后续 code_update 覆盖这个占位，前端拿到的 VFS 就是这个 {} 。

修复 ：直接删掉这条占位 code_update 。UI 已经通过前面的 phase=analyzing 状态事件知道生成开始了，不需要伪造一个假 VFS。
 2. 前端根因（占位为什么会变成 index.html = "{}" ）
fullstackBundler.ts L19 里 parseProjectCode("{}") ：解析成功但 entries.length === 0 ，返回 null 。调用方 CodeWorkspace.tsx L492 检测到 null ，走兜底 splitHtmlToVFS(code) ，把字符串 "{}" 当成单文件 HTML 内容，生成 { index.html: "{}" } ，右侧就显示 index.html = "{}"。

修复 ：去掉 entries.length === 0 ，让空 VFS {} 合法返回（不触发 splitHtmlToVFS 兜底）。
 3. 后端辅助防御（减少信息混淆）
App.py clean_generated_vfs 新增空 {} 早返回：直接 raise ValueError("VFS JSON object is empty — no files generated.") ，报错信息比 fullstack_bootstrap missing required files (5) 更能准确反映"模型没输出内容"，后续排查一目了然。

## 1. 核心结论
21 个回归测试全部通过 （21 passed, 0 failed），Diagnostics 无报错。代码修复已到位。

## 2. 修复点回顾（防止再出 envelope 泄漏）
入口函数 修复点 位置 generate_code_stream stream_json_completion → normalize_agent_envelope → 取 payload.html ；兼容 fullstack_bootstrap 时 payload=VFS 字典则提 frontend/index.html App.py#L2156-L2280 fix_code_stream 入口先用 _extract_html_from_mangled_envelope_source 剥掉被旧 bug 污染的 code；再 normalize 取 envelope App.py#L2283-L2460 fullstack_generate_stream stream_json_completion → normalize → resolve_agent_terminal_commands → 从 payload 里提 VFS（3+ 文件路径 key / files+deleted）；fallback 才走 clean_generated_vfs App.py#L2845-L2998 fullstack_patch_stream Path A 每次尝试都先 normalize_agent_envelope → resolve_agent_terminal_commands → answer/ask_clarification 直接返回； fullstack_bootstrap 且 payload 是 VFS 字典直接接 App.py#L3002-L3110 clean_generated_vfs 最外层安全网 ：首步就 normalize_agent_envelope 剥 envelope → 从 payload 提 VFS；兜底时提 payload.html 构造伪 VFS（5 文件 stub）；最后才 fallback 到 _extract_largest_json_object App.py#L1494-L1570 fullstack_bootstrap intent 确认已在白名单（ ALLOWED_INTENTS 第 148 行） App.py#L148

### 1. 核心结论与代码修改（6 处）
Bug 根因 ： generate_code_stream 历史 bug 把 UNIFIED ENVELOPE 的 整段 JSON 外壳 （甚至是被截断的半截 JSON，结尾 \"}} 都没闭合）直接写进了 index.html 。当用户点"请你继续修复"走 /api/code/modify → modify_code_stream 时，传入的 code 参数就是这堆"破损 JSON 外壳 + 半截 HTML 字符串字面量"的 烂数据 。
于是：

- 模型拿到的 <current code> 不是真实 HTML，生成的 operations[].target （如 .rank-num {\n width: 24px; ... ）是它脑补的完整页面内容 → 在烂源码里 100% 匹配不到
- apply_edit_operations 返回 unchanged → ensure_changed 抛异常 → 两次重试都失败 → 旧 fallback 只剥 ```html 围栏，但 content 是 envelope JSON，根本没有 HTML → 最后只 yield type: error 事件
- 而 stream_json_completion 产生的 agent_activity channel=output 事件会把 完整 envelope JSON 显示在"完整模型输出"大黑框里 → 用户体感就是" 完整 JSON 输出到外面了，源码一个字都没改 "。 Fix 1：收紧 _looks_like_full_html —— 必须真的以 HTML 开头
_looks_like_full_html

Why：破损 envelope JSON（ {..."html":"<!DOCTYPE html>..."}} ）在字符串 中间 包含 <!DOCTYPE 子串，旧版判定为"这就是 HTML 文档" → clean_generated_html 直接返回外壳，永远不会提取 payload。
 Fix 2：新增 _extract_html_from_mangled_envelope_source —— 破损 JSON 字面量强提取
_extract_html_from_mangled_envelope_source

三层策略，都失败就原样返回（绝不误伤）：

1. 非 { 开头 → 走 clean_generated_html （正常合法 JSON 或围栏）
2. 正则抓字面量 ： r'"html"\s*:\s*"((?:\\.|[^"\\])*)"' ，即使 JSON 整体 没有闭合 ，字符串字面量闭合了就能提取，用 json.loads('"' + raw + '"') 做反转义，天然支持 \" \n \uXXXX \\
3. 同构兜底 "code" 字段
只接受 _looks_like_full_html && len>300 ，避免把某个小 div 当整页替换。
 Fix 3： clean_generated_html 解析失败回落到强提取
clean_generated_html

旧的 try/except: pass 改成 except: recovered = _extract_html_from_mangled_envelope_source(cleaned); if recovered is not cleaned: return recovered 。
 Fix 4： fix_code_stream / modify_code_stream 入口先归一化 code
两处都在函数开头加：

- fix_code_stream 入口
- modify_code_stream 入口
Why 必须放在入口而不是后面解析失败了再补 ：如果现在就把被污染的烂 code 洗干净，后续喂给模型的 <current code> 就是合法 HTML → 模型输出的 operations target 才能真正匹配到源码里的 anchor，补丁本身就能一次成功，根本不会走到 fallback 分支。fallback 只是保险。
 Fix 5：patch 失败后，fallback 顺序改成"先 envelope.payload.html，再围栏兜底"
旧实现（modify/fix 末尾 fallback）：直接 re.sub(r"^```html...", content.strip()) ，但现在 content 是 envelope JSON，永远不是 HTML → 100% 失败。
新实现（modify + fix 两路径都加）：

```
try normalize(content)
  → payload.html / payload.code
  （_fix_content_newlines 修换行）
  → 否则 payload.operations 中是否是"单条 
  replace 且 content 是完整 HTML >500"
  → 否则 ```html 围栏兜底
  → reject_destructive_patch 校验
  → code_update 输出
```
- modify_code_stream 末尾 fallback
- fix_code_stream except 分支 fallback


## 原因（已确认，非猜测）
实测 GLM-5-turbo（用你当前配置）：

请求方式 正文 content 结果 stream + json_object 0 只吐思考，无正文 stream + json_object + 关思考 0 依然无正文 stream （不带 json_object） ✅ 正常 输出完整代码

根因 ：全栈走 stream_json_completion ，它带了 response_format={"type":"json_object"} 。GLM-5-turbo 在「json_object + 流式」下 content 恒为空（全部内容塞进 reasoning_content ），于是累积结果是空串 → normalize_agent_envelope 变成 intent=answer 空文本 → fullstack_generate_stream 走 answer 分支直接 return 。

为什么前端正常、全栈不行 ：前端 HTML 走 stream_html_completion （无 json_object），全栈为了强制 JSON 加了 json_object，恰好踩中 GLM 的坑。

## 修复内容（App.py）
1. stream_json_completion ：对 GLM 家族禁用 json_object ，改由 prompt + normalize_agent_envelope 提取 JSON；DeepSeek 保留；顺带累积 reasoning_content 兜底。
2. 次生问题（截断） ：去掉 json_object 后模型能正常输出了，但"简易教学系统"超过 max_tokens ，JSON 被截断（ database.json 没写完）。已把全栈 max_tokens 提到 16000。
3. 截断恢复 ：新增 _recover_truncated_fullstack_vfs + _ensure_valid_database_json ——从被截断 JSON 里抠出完整文件，若 database.json 缺失/是坏 JSON，则从 server.py 的 /api/<resource> 路由自动合成最小空库补齐，保证 validate_fullstack_vfs 通过。

## 这个 Maximum update depth exceeded 的原因
不是后端模型问题，是 CodeWorkspace 里几个 useEffect 的依赖数组不稳定，触发了 setState 死循环 。

具体路径：

1. 全栈生成跑通后， useCodeAutoRepair.commitAgentTrace 每次收到事件都会 setAgentRuns((previous) => previous.map(...)) ，产生一个全新的 agentRuns 数组。
2. CodeWorkspace 里这个 effect：
agentRuns 每轮都是新数组 → effect 每轮都触发 → setExpandedRunIds 被传入一个新的 Set 对象 → React 认为 state 变了 → re-render → agentRuns 又变新数组 → 循环。

repairLogs 那个 effect 同理。

## 修复内容
在 CodeWorkspace.tsx ：

- 展开 agentRuns / repairLogs 的 Set 前先检查 id 是否已存在，存在就返回原引用，避免无意义的状态更新。
- 顺手给 Terminal Tab 自动选中的 effect 加了 target.id !== activeTerminalRunId 守卫。
```
setExpandedRunIds((previous) => {
  if (previous.has(latest.id)) return 
  previous;
  return new Set(previous).add(latest.
  id);
});
```

## 回答：reasoning_content 为空是因为流式吗？
不完全是。 根因是三个因素叠加：

1. GLM 的思考模式默认开启 （ model_settings.json 里 thinking_enabled: true ）→ 模型会先走 reasoning_content 吐思考过程
2. 任务复杂度高 → 模型思考量大。我的诊断测试里，简单 prompt（"做个教学系统"）GLM 流式能正常输出 6304 字 content；但补丁流程的 FULLSTACK_PATCH_SYSTEM_PROMPT 很复杂（要求精确片段匹配），GLM 思考了 24037 字
3. max_tokens 限制总输出 （reasoning + content 共享预算）→ 补丁模式 max_tokens=6000 ，GLM 思考就花光了，content 自然为空
流式本身不是根因。 流式 + 简单 prompt 时 GLM 能正常输出 content。是「复杂 prompt + 思考模式 + 小 max_tokens」三者叠加导致思考吃光了预算。

## 改动计划
### 整体流程
```
用户提交修改指令
       ↓
后端规则判断（关键词/目标文件数）→ 需要拆解？
       ↓ 是                    ↓ 否
LLM 生成任务列表              直接走原有单
步 patch
[{id, title, target_files,    ↓
  description, status}]      apply + 
  yield
       ↓
逐个执行子任务：
  1. 构建 focused prompt（当前VFS + 该任务
  描述）
  2. stream_json_completion
  （max_tokens=2000）
  3. content 为空？→ 非流式重试该步
  4. 解析 patch → apply_file_replace/
  apply_operations
  5. 成功 → yield code_update + 标记完成
     失败 → 标记 failed + 跳过 + 继续
       ↓
全部完成 → yield 最终 VFS + summary（含失败
项汇报）
```

## 关键技术结论
GLM-5/5.1/5.2 支持"轮级思考"（Turn-level Thinking） ——可以在每一轮独立开关思考。这对你的场景是完美方案：

- 任务拆解那步 （模型要分析整体需求、规划子任务）→ 开启思考，让模型想清楚
- 每个子任务执行那步 （prompt 已写得很具体，只需输出 patch）→ 关闭思考，直接输出，不耗 reasoning token
这正好对应文档里说的：
 "对问个事实/改个措辞等轻量轮次可关闭思考，追求快速响应；对复杂规划/多约束推理/代码调试等重任务轮次可开启思考"
## 具体实现
stream_json_completion 加一个 thinking 参数，传递给 GLM 的 extra_body ：

```
async def stream_json_completion(
    client, model, messages, ...,
    thinking: str = "enabled",  # 
    enabled / disabled
):
    ...
    if is_glm:
        kwargs["extra_body"] = 
        {"thinking": {"type": thinking}}
```
然后：

- 子任务执行 ： thinking="disabled" → 不耗 reasoning token，直接输出 patch
- 任务拆解 + 完整生成 ： thinking="enabled" → 保留深度思考
这样后端的 patch 场景再也不会被 reasoning 占光预算。

## 改动总结
轮级思考方案已落地 ：

调用点 thinking 参数 效果 子任务执行 "disabled" 关闭思考，直接输出 patch，不耗 reasoning token 任务拆解 / 完整生成 / 补丁 "enabled" （默认） 保留深度思考

### 改动点
1. stream_json_completion 加 thinking: str = "enabled" 参数
2. GLM 模型时通过 extra_body={"thinking": {"type": thinking}} 传递
3. 子任务执行处传 thinking="disabled"
### 现在子任务的行为
```
拆解任务（思考开启）→ 输出 3 个子任务列表
子任务1（思考关闭）→ 直接输出该文件的 
file-replace patch
子任务2（思考关闭）→ 直接输出
子任务3（思考关闭）→ 直接输出
```
这样 GLM 子任务不再有长篇 reasoning_content ， max_tokens=4000 全部用于输出 code， content 不会再为空，patch 也能正常解析应用。

## 根因
GLM 模型在全栈生成时，输出格式是：

```
根据您的需求，我将为您创建...

{ "intent": "fullstack_bootstrap", 
"payload": { ... } }
```
前面有 自然语言前缀 ，导致 json.loads 解析失败。 normalize_agent_envelope fallback 成 intent=answer ，于是 fullstack_generate_stream 走了 answer 分支，前端显示"未改动代码"。

## 修复
1. 新增 _extract_embedded_json() ：按括号匹配从文本中提取第一个 {...} / [...] ，并校验是否像 envelope。
2. 在 normalize_agent_envelope 字符串解析分支里，JSON 直接解析失败后先尝试提取嵌入 JSON，再 fallback 到破损恢复 / answer。
3. 新增单元测试覆盖该场景。


## 422 原因
生成失败时代码区是 {} ， parseProjectCode('{}') 返回空对象 {} 。在 JS 中 {} 是 truthy，所以前端误判为"有全栈项目"，调用 /api/code/fullstack/modify 时传了空 vfs ，后端 FullstackModifyRequest.vfs 的 min_length=1 校验失败，返回 422。

## 修复
1. 前端 useCodeAutoRepair.ts ：modify 和 fix 路径都改判断为 hasVfs = currentVfs && Object.keys(currentVfs).length > 0 ，空 VFS 不再走 fullstack 接口。
2. 后端 App.py ：给 vfs 校验加了注释说明。

## 根因
[CodeWorkspace.tsx](file:///d:/AI-Agent学习计划/AI-Agent study/frontend/ai-agent/src/components/CodeWorkspace.tsx) 控制台面板固定 consoleHeight=144px ，切到终端 Tab 后，减去 Tab 头 + 提案横幅 + 会话条，"终端 host" 被 flex 链压到 offsetHeight=0 。 tryOpen() 检测到 0 就无限跳过， term.open() 从未成功 → 终端永远空白、无法输入。这解释了日志里 skip: host size 0x0 1423 0 （宽度正常、高度塌陷）。

## 修复（3 处）
1. 面板加高 ：终端 Tab 活动时面板高度改为 320px （原来 144px），保证有足够渲染空间。
2. 绝对填充 ：终端内容区改为 relative flex-1 overflow-hidden ， IntegratedTerminal 用 absolute inset-0 包裹 + 根节点从 flex-1 改为 h-full ，不再依赖多层 flex-1 传递高度。
3. 最小高度兜底 ：xterm host 加 min-h-[120px] ，即使 flex 再次塌陷， term.open() 也一定能拿到非零尺寸。

## 发生了什么
之前修好了"终端 host 高度为 0"的问题（ min-h-[120px] 兜底 + 面板加高到 320px），所以 term.open() 现在能真正执行了。但暴露出 xterm 的 经典竞态 ：从 Console Tab 切到 Terminal Tab 时，容器 display 刚从 none → block 的 同一帧内 同步调用 term.open() ，xterm 内部 Viewport._innerRefresh 读取 buffer.dimensions 得到 undefined → 抛 TypeError 。这正是 [IntegratedTerminal.tsx](file:///d:/AI-Agent学习计划/AI-Agent study/frontend/ai-agent/src/components/IntegratedTerminal.tsx#L163-L166) 注释早已记载的那个坑。

日志佐证：错误出现在 [terminal][ws] connecting 和 [terminal][ws] OPEN 之间——即首次同步 tryOpen() 抛错，随后 rAF/timeout 重试成功，终端才连上。

## 修复
把 term.open() 从同步执行改为 延后到下一帧 （ requestAnimationFrame ）执行，并用 openScheduled 防止重复调度。这样无论从哪个 Tab 切入， term.open() 都在布局与字体测量完成后才运行，不再踩同帧竞态。


## 根因
三个问题都是同一个原因： 前端用相对路径请求后端 API，但 Next.js dev server 在 3000 端口，后端在 8000，请求全部 404 。

- 关闭按钮 fetch('/api/terminal/close/...') → 打到 3000 → 404
- 手动终端 WebSocket ws://localhost:3000/ws/terminal/... → 连不上 → 看不到 proposition
- 审批横幅同理，WebSocket 没连上就收不到广播
## 修复
1. next.config.ts ：加了 rewrites() 把 /api/* 转发到 http://localhost:8000/api/* ，关闭按钮等 HTTP 请求自动生效。
2. IntegratedTerminal.tsx ： buildWebSocketUrl 改用 API_BASE_URL （ http://localhost:8000 ）推导 WS 地址，不再依赖 window.location.host （那是 3000）。Next.js rewrites 不代理 WebSocket，所以必须直连后端端口。
## 注意
next.config.ts 改动后需要 重启 Next.js dev server （ npm run dev ）才能生效。重启后：

- 关闭按钮 → 正常 200
- "+ 我的终端" → WebSocket 连上 8000，终端可用
- Agent 终端命令 → SSE 事件触发 Tab 切换，WebSocket 连上收到 proposition，审批横幅弹出

## 问题 1：终端不能输入/没输出
根因 ： _pty_reader 子线程里 asyncio.get_event_loop_policy().get_event_loop() 会创建一个新的事件循环，不是 FastAPI 的主循环。所以 run_coroutine_threadsafe 把协程投到了一个永远不会运行的循环上，PTY 输出永远推不到前端，终端看起来是死的。

修复 terminal_service.py ：

- PtyTerminalPool 新增 _event_loop 字段 + capture_event_loop() 方法
- attach_websocket （async 上下文）里调用 capture_event_loop() 捕获主循环
- 新增 _schedule_broadcast() 方法，用捕获到的主循环投递协程
- _pty_reader 改用 _schedule_broadcast() ，不再自己 get_event_loop()
## 问题 2：没有审批弹窗
根因 ： resolve_agent_terminal_commands 会阻塞等待用户审批，但之前没有发 SSE 事件通知前端。前端不知道有提案来了，不会切到终端 Tab，WebSocket 没连上，proposition 广播没人接收，90 秒后超时。
修复 ：
- 后端 App.py ：4 个 resolve_agent_terminal_commands 调用点之前，先 yield format_sse({"type": "terminal_proposal", "command": ..., "run_id": run_id})
- 前端 useCodeAutoRepair.ts ：收到 terminal_proposal SSE 后， window.dispatchEvent(new CustomEvent('terminal-proposal-arrived', {detail: {run_id}}))
- 前端 CodeWorkspace.tsx ：监听 terminal-proposal-arrived ，自动 setActiveTerminalTab('terminal') + setActiveTerminalRunId(run_id)
- 前端 api.ts ： TerminalProposalEvent 加 run_id?: string 字段


根因 ： fullstack_patch_stream 的三条路径（Path A 精确补丁 / Path B 文件重写 / 子任务循环）调用 resolve_agent_terminal_commands 阻塞等审批前， 都没发 terminal_proposal SSE 事件 。前端 CodeWorkspace.tsx 监听 terminal-proposal-arrived CustomEvent 才会自动切到终端 Tab 显示审批横幅——没事件 → 用户在别的 Tab 看不到横幅 → 90s 超时 → 命令不执行。 generate_code_stream 、 fullstack_generate_stream 、 modify_code_stream 都有这步，唯独 patch 漏了。
修改 （App.py）：
1. resolve_agent_terminal_commands 加诊断日志（统一前缀 [terminal_cmd] ）：入口打印 commands 数/pool 类型/has_propose_command；空 commands、pool 不可用、command 为空各打 warning；每条提案前后打 info（含 status/exit_code/snippet）；异常用 logger.exception 带完整堆栈。
2. Path A ：补发 terminal_proposal SSE + 加日志；envelope 解析失败也打异常日志。
3. Path B ：同上补发。
4. 子任务循环 ：同上补发。
验证 ： python -m py_compile App.py 通过。
下次复现时怎么看日志 ：在终端或控制台过滤 [terminal_cmd] ，能直接看到：
- 模型是否真的输出了 terminal_commands （count=0 说明模型没给）
- pool 是否传进来了（ pool=None 或 has_propose_command=False 说明上游没传）
- propose 是否被调起（没看到"提案 #1 开始"说明 SSE 事件链路仍有问题）
- 用户决策结果（approved/rejected/timeout/blocked）

## 根因
[CodeWorkspace.tsx](file:///d:/AI-Agent学习计划/AI-Agent study/frontend/ai-agent/src/components/CodeWorkspace.tsx) 控制台面板固定 consoleHeight=144px ，切到终端 Tab 后，减去 Tab 头 + 提案横幅 + 会话条，"终端 host" 被 flex 链压到 offsetHeight=0 。 tryOpen() 检测到 0 就无限跳过， term.open() 从未成功 → 终端永远空白、无法输入。这解释了日志里 skip: host size 0x0 1423 0 （宽度正常、高度塌陷）。

## 修复（3 处）
1. 面板加高 ：终端 Tab 活动时面板高度改为 320px （原来 144px），保证有足够渲染空间。
2. 绝对填充 ：终端内容区改为 relative flex-1 overflow-hidden ， IntegratedTerminal 用 absolute inset-0 包裹 + 根节点从 flex-1 改为 h-full ，不再依赖多层 flex-1 传递高度。
3. 最小高度兜底 ：xterm host 加 min-h-[120px] ，即使 flex 再次塌陷， term.open() 也一定能拿到非零尺寸。

## 发生了什么
之前修好了"终端 host 高度为 0"的问题（ min-h-[120px] 兜底 + 面板加高到 320px），所以 term.open() 现在能真正执行了。但暴露出 xterm 的 经典竞态 ：从 Console Tab 切到 Terminal Tab 时，容器 display 刚从 none → block 的 同一帧内 同步调用 term.open() ，xterm 内部 Viewport._innerRefresh 读取 buffer.dimensions 得到 undefined → 抛 TypeError 。这正是 [IntegratedTerminal.tsx](file:///d:/AI-Agent学习计划/AI-Agent study/frontend/ai-agent/src/components/IntegratedTerminal.tsx#L163-L166) 注释早已记载的那个坑。

日志佐证：错误出现在 [terminal][ws] connecting 和 [terminal][ws] OPEN 之间——即首次同步 tryOpen() 抛错，随后 rAF/timeout 重试成功，终端才连上。

## 修复
把 term.open() 从同步执行改为 延后到下一帧 （ requestAnimationFrame ）执行，并用 openScheduled 防止重复调度。这样无论从哪个 Tab 切入， term.open() 都在布局与字体测量完成后才运行，不再踩同帧竞态。

## 根因
手动终端和后端发的 sessions 列表绑定，close 后后端从 _terminals 移除，下一次 list 广播让 Tab 消失；而 智能体终端 Tab 是从持久化的 agentRuns 派生的 （见 [IntegratedTerminal.tsx 482-501](file:///d:/AI-Agent学习计划/AI-Agent study/frontend/ai-agent/src/components/IntegratedTerminal.tsx#L482-L501)），后端 close 成功（日志 200 ok=true）但 agentRuns 里该 run 仍在 → Tab 删不掉、名字回退到 agentRuns 里的请求标题。

## 修复
在 IntegratedTerminal 加 closedAgentRunIds 集合：
1. 点 × 时把 run_id 记录进集合（并照常调用后端 close）。
2. allSessions 合并列表过滤掉已关闭的 run，后端 sessions 和 agentRuns 两条来源都排除。
效果：智能体终端点 × 后 Tab 立即从列表消失，与手动终端行为一致。

## 根因
日志里大量 [Fast Refresh] rebuilding 。Fast Refresh 会 重建组件并 dispose 旧 xterm 实例 ，但因为 termReady state 被保留为 true，依赖它的 WebSocket effect 不会重跑 。结果：

- WS 还连着，但 onmessage 闭包里的 term 是 已 dispose 的旧实例
- term.write() 写入失效对象 → 数据明明推到了（日志有 pty_output），但渲染空白
- 同理， onData 输入绑定在旧 term 上，新 term 没绑 → 键盘输入无反应，只有光标闪
## 修复（IntegratedTerminal.tsx）
1. 输出用最新实例 ： onmessage / onclose / error 里所有 term.write/writeln 改为 termRef.current ，每次读取当前存活的 term，Fast Refresh 重建后也能写入新实例。
2. 输入绑定移到 term 创建处 ： onData 在 doOpen （term.open 成功后）时绑定并存入 inputDisposableRef ，Fast Refresh 重建新 term 时重新接线，键盘输入始终可用。
3. 清理 ：移除 WS effect 里重复的 onData 绑定（避免按键重复发送），cleanup 时 dispose 输入回调。

## 根因（已定位）
浏览器实测发现页面里有 两个 .xterm 实例同时叠加 在同一个 host 容器里：

- 实例 0： 1006×496 ，24 行， 全空白 （新创建的，覆盖在上层）
- 实例 1： 1006×103 ，5 行， 有真实内容 （Windows PowerShell 等，被压扁遮挡在下层）
所以数据其实都在（所以你看到日志 pty_output Windows PowerShell 正常推送），但视觉上被一个巨大的空白实例遮住了 → "一片空白"。

## 为什么会叠加
term.dispose() 只清理 xterm 内部 buffer/监听器， 不会移除 term.open() 时 append 到 host 的 .xterm DOM 节点 。当 Fast Refresh 重建组件（日志里多次 [Fast Refresh] rebuilding ）或切换终端重挂载时，cleanup 后旧的 .xterm 残留，新的 term.open(host) 又 append 一个 → 两个实例叠加。

## 修复
在 doOpen() 里 term.open() 前，先清空 host 内残留的 .xterm 节点：

```
host.querySelectorAll(':scope > .xterm').forEach((n) => n.remove());
term.open(host);
```
这样无论组件重建多少次，host 里始终只有一个 xterm 实例，不会叠加。

## 根因
点击"+ 我的终端"只在前端设置了 activeTerminalRunId ，但 新 runId 还没进入后端 sessions 列表 。我新加的"多实例"渲染区只遍历 allSessions 生成 host div，新 runId 没有对应 host div → term 找不到宿主 → 尺寸 0×0 → 终端空白。

## 修复
在 allSessions 生成逻辑里预注入当前 activeRunId （如果它还没出现在后端 sessions 或 agentRuns 里），确保渲染区立即生成对应的 host div。

## 已完成：命令自动落到落盘目录
改动（ App.py ）：

1. 新增 _generated_project_dir(run_id) ：返回落盘根 工程根/generated/<run_id>/ 。
2. 新增 _remap_command_to_generated_dir(command, run_id) ：若命令以 cd <子目录> 开头且落盘目录存在，则重写为 cd <落盘根> ; <原命令> ，让模型写的 cd backend && python server.py 相对落盘根执行。
3. 在 resolve_agent_terminal_commands 提案前调用该重写。
效果 ：命令 cd backend && python server.py 会被重写为：

```
cd "D:\AI-Agent学习计划\AI-Agent study\generated\agent-run-xxx" ; cd backend && 
python server.py
```
先切到落盘根，再 cd backend → python server.py 就能找到 generated/<run_id>/backend/server.py 并启动。
